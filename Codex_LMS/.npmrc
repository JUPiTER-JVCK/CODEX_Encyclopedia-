# `engines` in package.json only warns by default. The Node floor here is
# real — vite@8 will not run below 20.19 — so fail the install rather than
# printing a warning nobody reads and breaking further downstream.
engine-strict=true
